# Frontend env — these are SAFE to expose (no secrets here)
# In dev, the Vite proxy handles /api routing automatically
# In production, set this to your deployed backend URL
VITE_API_BASE_URL=http://localhost:3001
